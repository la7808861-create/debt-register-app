{
  "name": "debt-register-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "build:apk": "cd android && .\\gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
